#!/usr/bin/node
const process = require('process');

if (proccess.argv.length <= 2) {
  console.log('No argument');
} else if (proccess.argv.length === 3) {
  console.log('Argument found');
} else {
  console.log('Argument found');
}
